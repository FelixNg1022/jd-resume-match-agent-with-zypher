# Job Search AI Agent with Zypher

A simple AI agent built using CoreSpeed's Zypher framework.  
Includes both:
- CLI interface
- Minimal browser-based GUI

## Run CLI Version
deno run -A main.ts

## Run GUI Version
deno run -A server.ts
open http://localhost:8000
